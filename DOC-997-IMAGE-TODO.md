# DOC-997 Image Files Note

## Current Status

The documentation now references existing image files:
- `public/docs/output/img/data-tables/add-nested-groupby-icon.png` (already exists)
- `public/docs/output/img/data-tables/nested-groupby-png.png` (already exists)

## Optional: Use New Screenshots from Jira

The Jira ticket DOC-997 has two new screenshots attached that may be more up-to-date or clearer than the existing images:

1. **Jira attachment 116192** ("Capture d'écran 2025-11-17 à 10.28.33.png")
   - Shows the "Add top-level group-by" icon
   - Could replace: `add-nested-groupby-icon.png`

2. **Jira attachment 116191** ("Capture d'écran 2025-11-17 à 10.28.56.png")
   - Shows multiple group-bys result
   - Could replace: `nested-groupby-png.png`

If the new screenshots are better, download them from the Jira ticket and replace the existing files.

## Jira Ticket
https://amplitude.atlassian.net/browse/DOC-997
