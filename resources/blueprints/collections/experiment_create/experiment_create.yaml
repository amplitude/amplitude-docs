title: 'Experiment Create'
tabs:
  main:
    display: Main
    sections:
      -
        fields:
          -
            handle: group
            field:
              type: select
              display: Group
              instructions: 'Which group does this article belong to? Group defines the third fourth segment in the URL, after `create`.'
              localizable: false
              options:
                -
                  key: feature
                  value: null
                -
                  key: web
                  value: null
          -
            import: help_center_common
            config:
              title:
                required: true
  sidebar:
    display: Sidebar
    sections:
      -
        fields:
          -
            handle: slug
            field:
              type: slug
              localizable: true
              validate: 'max:200'
