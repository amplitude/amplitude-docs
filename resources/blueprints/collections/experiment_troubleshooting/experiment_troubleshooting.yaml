title: 'Experiment Troubleshooting'
tabs:
  main:
    display: Main
    sections:
      -
        fields:
          -
            import: help_center_common
            config:
              title:
                required: true
  sidebar:
    display: Sidebar
    sections:
      -
        fields:
          -
            handle: slug
            field:
              type: slug
              localizable: true
              validate: 'max:200'
          -
            handle: source
            field:
              type: text
              display: Source
