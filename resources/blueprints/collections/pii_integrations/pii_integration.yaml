title: 'PII Integration'
tabs:
  main:
    display: Main
    sections:
      -
        fields:
          -
            import: help_center_common
            config:
              title:
                required: true
  integration_metadata:
    display: 'Integration Metadata'
    sections:
      -
        display: 'New Section'
        fields:
          -
            import: integration_category
  sidebar:
    display: Sidebar
    sections:
      -
        fields:
          -
            handle: slug
            field:
              type: slug
              localizable: true
              validate: 'max:200'
