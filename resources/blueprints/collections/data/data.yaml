title: Data
tabs:
  main:
    sections:
      -
        fields:
          -
            import: help_center_common
            config:
              title:
                required: true
  sidebar:
    sections:
      -
        fields:
          -
            handle: slug
            field:
              type: slug
              localizable: true
              validate: 'max:200'
          -
            handle: parent
            field:
              type: entries
              collections:
                - data
              max_items: 1
              listable: false
              localizable: true
