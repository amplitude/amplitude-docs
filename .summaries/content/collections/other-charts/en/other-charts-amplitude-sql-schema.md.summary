**Summary:** Documents Amplitude SQL's table schema for the $events table and merged users table in Snowflake, including special field shortcuts ($date, $events, $amplitude_id) that integrate with Amplitude's datepicker and automatically handle merged user mappings.

**Keywords:**
- Amplitude SQL
- $events table schema
- Snowflake database
- field shortcuts
- $date datepicker integration
- $amplitude_id merged users
- merged users table
- event_time timestamp
- client_event_time
- server_received_time
- amplitude_id mapping
- one-table schema
- VARIANT data type
- UTC timezone handling
- device_id tracking
- session_id milliseconds epoch