**Summary:** This migration guide documents the transition from Amplitude's maintenance Node.js SDK (`@amplitude/node`) to the latest Node.js SDK (`@amplitude/analytics-node`), detailing API changes including initialization with `init()`, configuration object differences, `logEvent()` to `track()` mapping, `identify()` signature changes, and middleware to plugins architecture conversion.

**Keywords:**
- @amplitude/node
- @amplitude/analytics-node
- Node.js SDK migration
- init() initialization
- logEvent() to track() mapping
- identify() signature changes
- middleware to plugins conversion
- flushQueueSize configuration
- EnrichmentPlugin
- DestinationPlugin
- EventOptions
- plugin architecture
- HTTP V2 API
- Batch API support
- transportProvider
- flushIntervalMillis