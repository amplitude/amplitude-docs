**Summary:** This documentation covers migrating from Amplitude's maintenance Node.js SDK (`@amplitude/node`) to the latest Node.js SDK (`@amplitude/analytics-node`), detailing API changes including initialization with `init()`, event tracking with `track()` replacing `logEvent()`, configuration object differences, and middleware-to-plugins architecture conversion.

**Keywords:**
- @amplitude/node
- @amplitude/analytics-node
- Node.js SDK migration
- init() initialization
- track() API
- logEvent() replacement
- middleware to plugins conversion
- EnrichmentPlugin
- DestinationPlugin
- identify() signature changes
- EventOptions
- flushQueueSize
- flushIntervalMillis
- transportProvider
- plugin architecture
- namespace import
- named import
- configuration object migration