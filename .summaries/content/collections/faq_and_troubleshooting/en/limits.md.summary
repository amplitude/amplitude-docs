## Summary
This documentation covers Amplitude's data ingestion and query limits including monthly event volume limits with overage fees and account blocking policies, instrumentation limits for event types (2000), event properties (2000), and user properties (1000) that affect data indexing and querying, chart-specific date range and segmentation limits, and CSV export row limits that vary by chart type.

## Keywords
- monthly event volume limits
- overage fees
- account blocking policy
- instrumentation limits
- event types limit 2000
- event properties limit 2000
- user properties limit 1000
- data indexing restrictions
- string character limit 1024
- Event Segmentation chart limits
- inline behavioral cohorts 3-year range
- breakdown table 30 segments
- CSV export row limits
- group-by limits
- Export API raw data access
- Amplitude Data event type deletion
- orange red warning indicators
- microscope download users 1 million rows