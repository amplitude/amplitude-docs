# Summary
The Export API allows developers to retrieve event data from Amplitude projects with a GET request to `/api/2/export`, supporting time-based filtering with required start/end parameters in YYYYMMDDTHH format, and returning zipped JSON files with detailed event information, subject to a 4GB size limit and 2-hour data availability delay.

# Keywords
- event data export
- server_upload_time
- 4GB size limit
- YYYYMMDDTHH time format
- JSON event schema
- UTC timestamps
- export delay
- cross-project limitations
- 365-day maximum query period
- HTTP basic authentication