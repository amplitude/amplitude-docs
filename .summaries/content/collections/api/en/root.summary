## Amplitude Analytics API Platform

Amplitude provides a comprehensive suite of REST APIs for analytics data ingestion, user management, and data retrieval across their analytics platform. The API ecosystem supports real-time event tracking, user behavior analysis, cohort management, and enterprise-grade data governance features.

## Product Architecture and Relationships

The Amplitude API platform consists of several interconnected components organized around core analytics workflows:

**Data Ingestion Layer:**
- **HTTP V2 API** (`/2/httpapi`) serves as the primary event ingestion endpoint for server-to-server tracking
- **Batch Event Upload API** handles bulk data uploads with 20MB payload limits and rate limiting (1000 events/second per device)
- **Attribution API** (`/attribution`) processes mobile advertising attribution events using advertising IDs (IDFA, ADID, etc.)

**User and Group Management:**
- **Identify API** (`/identify`) manages user property updates and User ID to Device ID mappings without generating events
- **Group Identify API** (`/groupidentify`) handles group-level properties for account-based analytics
- **User Mapping API** (`/usermap`) provides cross-project user aliasing through global user IDs

**Data Retrieval and Export:**
- **Export API** (`/api/2/export`) enables bulk event data extraction in zipped JSON format with 4GB limits
- **Dashboard REST API** provides structured access to analytics data including funnels, retention, and segmentation
- **User Profile API** (`profile-api.amplitude.com/v1/userprofile`) retrieves comprehensive user profiles with computed properties and recommendations

**Analytics Enhancement:**
- **Behavioral Cohorts API** (v3/v5) manages user cohorts with asynchronous S3-based download workflows
- **Lookup Table API** (v1/v3) augments event and user properties through CSV-based property mappings
- **Taxonomy API** provides enterprise schema management for events, properties, and classifications

## Key Nomenclature and Definitions

**Core Identifiers:**
- `amplitude_id`: Amplitude's internal user identifier
- `user_id`: Customer-defined user identifier
- `device_id`: Device-specific identifier for anonymous tracking
- `session_id`: Session tracking identifier
- `insert_id`: Event deduplication identifier

**Event Structure:**
- `event_type`: The name/category of the tracked event
- `event_properties`: Custom properties specific to the event instance
- `user_properties`: Properties associated with the user performing the event
- `group_properties`: Properties for group-level analytics (requires Accounts add-on)

**Property Operations:**
- `$set`: Set property value
- `$setOnce`: Set property only if not already set
- `$add`: Add numeric value to existing property
- `$append`/`$prepend`: Add values to array properties
- `$unset`: Remove property
- `$remove`: Remove specific values from arrays

**Rate Limiting and Throttling:**
- **EPDS**: Events Per Device per Second
- **EPUS**: Events Per User per Second
- Cost-based rate limiting for enterprise APIs
- HTTP 429 responses for throttling

## Product Ecosystem Integration

The API platform integrates with Amplitude's broader analytics ecosystem through several key mechanisms:

**Plan-Based Feature Access:**
- Growth/Enterprise plans unlock advanced cohort features and higher rate limits
- Accounts add-on enables group-level analytics
- Portfolio add-on provides cross-project user mapping
- Activation plan required for User Profile API access

**Regional Data Residency:**
- Standard endpoints: `api2.amplitude.com`, `api.amplitude.com`
- EU residency: `api.eu.amplitude.com`
- Specialized endpoints: `data-api.amplitude.com` (Lookup Tables), `profile-api.amplitude.com` (User Profiles)

**Compliance and Privacy:**
- **CCPA DSAR API** for data subject access requests with 3-day SLA
- **User Privacy API** for GDPR/CCPA deletion compliance with 30-day scheduling
- **SCIM 2.0 API** for enterprise user provisioning and identity management

## API Endpoints and Authentication

**Primary Ingestion Endpoints:**
- `POST api2.amplitude.com/2/httpapi` - Event ingestion
- `POST api2.amplitude.com/batch` - Batch event upload
- `POST api2.amplitude.com/attribution` - Attribution events
- `POST api2.amplitude.com/identify` - User property updates
- `POST api2.amplitude.com/groupidentify` - Group property updates

**Data Retrieval Endpoints:**
- `GET api.amplitude.com/api/2/export` - Event data export
- `GET profile-api.amplitude.com/v1/userprofile` - User profile retrieval
- Various Dashboard REST API endpoints for analytics queries

**Management Endpoints:**
- `POST/GET api.amplitude.com/api/2/annotations` - Chart annotations
- `POST api.amplitude.com/api/2/release` - Release tracking
- Taxonomy API endpoints for schema management
- Cohort API endpoints for user segmentation

**Authentication Methods:**
- API key authentication for ingestion endpoints
- HTTP Basic authentication for retrieval and management APIs
- Secret key authentication for User Profile API
- SCIM key authentication for user provisioning

The platform implements comprehensive rate limiting, supports both real-time and batch processing workflows, and provides extensive debugging and monitoring capabilities through detailed response codes and metrics APIs.