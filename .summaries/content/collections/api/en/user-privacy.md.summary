**Summary:** Documents the User Privacy API for GDPR/CCPA compliance, enabling programmatic deletion of user data via POST requests to `/api/2/deletions/users` with support for both project-level and organization-wide deletions using `delete_from_org` parameter.

**Keywords:**
- User Privacy API
- GDPR CCPA compliance
- `/api/2/deletions/users` endpoint
- amplitude_ids user_ids deletion
- delete_from_org parameter
- ignore_invalid_ids parameter
- HTTP basic authentication
- batch deletion jobs
- deletion job status (staging, submitted, done)
- rate limit 1 request per second
- 100 users per request limit
- 30-day deletion timeline
- warehouse CDC reingest warning
- setOptOut() method reference