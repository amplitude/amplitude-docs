**Summary:** Documents the User Privacy API for GDPR/CCPA compliance, enabling programmatic deletion of user data via amplitude_ids or user_ids with batch processing, rate limiting (1 req/sec, 100 users per request), and 30-day deletion scheduling with job status tracking.

**Keywords:**
- User Privacy API
- GDPR CCPA compliance
- amplitude_ids user_ids deletion
- /api/2/deletions/users endpoint
- delete_from_org parameter
- ignore_invalid_ids parameter
- batch deletion jobs
- 30-day deletion scheduling
- job status tracking (staging/submitted/done)
- rate limiting 1 req/sec 100 users
- HTTP basic authentication
- warehouse CDC reingest prevention
- setOptOut method
- project-level organization-level deletion
- admin email notifications