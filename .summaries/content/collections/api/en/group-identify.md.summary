# Summary
The Group Identify API allows setting or updating properties of particular groups in Amplitude, supporting operations like $set, $setOnce, $add, $append/$prepend, and $unset, with limits of 1024 group identifies per request, 1024 properties per request, and a 1MB maximum request size.

# Keywords
- group_identify
- group_properties
- group_type
- group_value
- property operations ($set, $setOnce, $add, $append, $prepend, $unset)
- Accounts add-on
- request limits
- API endpoints (api2.amplitude.com/groupidentify)