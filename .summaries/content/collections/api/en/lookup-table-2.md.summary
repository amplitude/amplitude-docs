**Summary:** Documents the Amplitude Lookup Table API 2 (v3) which provides REST endpoints for managing CSV-based lookup tables that augment user and event properties through property mappings, including CRUD operations, CSV upload/download, and incremental updates with HTTP basic authentication.

**Keywords:**
- Amplitude Lookup Table API v3
- data-api.amplitude.com/api/3/lookup_table
- CSV property mapping
- multipart/form-data upload
- HTTP basic authentication
- keyProperty groupType
- incremental CSV merge
- lookup table CRUD operations
- property augmentation
- case-sensitive exact matching
- 100MB file size limit
- 1,000,000 row limit