# Summary
The Batch Event Upload API allows uploading large amounts of event data (up to 20MB) to Amplitude, following the HTTP API format but with specific requirements for content-type and event payload structure, and includes rate limiting of 1000 events per second per device/user ID.

# Keywords
- batch-event-upload
- event ingestion
- payload size limit
- rate limiting
- throttling
- EPDS (events per device second)
- EPUS (events per user second)
- event deduplication
- insert_id
- error responses
- daily quota