## Summary

The Batch Event Upload API enables bulk uploading of large amounts of event data to Amplitude with a 20MB payload limit, using JSON format with `events` array and `application/json` content-type, supporting the same event schema as HTTP API v2 but with rate limiting of 1000 events/second per device/user ID.

## Keywords

- Batch Event Upload API
- bulk event ingestion
- 20MB payload limit
- application/json content-type
- events array parameter
- HTTP API v2 format
- rate limiting 1000 events/second
- device_id user_id throttling
- EPDS EPUS measurement
- 429 Too Many Requests
- 500,000 daily event limit
- event deduplication insert_id
- user_properties synchronization
- $identify $groupidentify operations
- api_key authentication
- /batch endpoint
- event_type event_properties
- session_id tracking
- revenue tracking fields
- group_properties accounts addon