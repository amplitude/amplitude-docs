## Summary

This documentation covers the Behavioral Cohorts API which provides endpoints for listing, exporting, downloading, uploading, and updating user cohorts in Amplitude using HTTP basic authentication with asynchronous download operations requiring three-step process (request, poll status, download file).

## Keywords

- Behavioral Cohorts API
- cohort export download
- asynchronous API polling
- HTTP basic authentication
- cohort upload BY_AMP_ID BY_USER_ID
- cohort membership ADD REMOVE operations
- Amazon S3 pre-signed URLs
- request_id status polling
- cohort sync metadata
- Growth Enterprise plan limits
- 500 requests per month limit
- 2 million user cohort size limit
- concurrency limit 5 requests
- user properties propKeys parameter
- cohort definition schema
- incremental membership updates
- skip_invalid_ids parameter
- count_group User ID types
- discoverable published cohorts