## Summary

This documentation covers the Behavioral Cohorts API (v3/v5) which provides endpoints for listing, exporting, uploading, and managing user cohorts in Amplitude, including asynchronous cohort download operations with request polling and file retrieval from S3.

## Keywords

- Behavioral Cohorts API
- cohort download asynchronous workflow
- cohort upload BY_AMP_ID BY_USER_ID
- cohort membership incremental updates
- request_id polling mechanism
- Amazon S3 pre-signed URLs
- cohort sync metadata
- API rate limits 500 requests per month
- concurrency limit 5 requests
- maximum cohort size 2 million users
- HTTP basic authentication
- /api/3/cohorts and /api/5/cohorts endpoints
- cohort_id and app_id parameters
- user properties propKeys inclusion
- ADD REMOVE membership operations
- skip_invalid_ids parameter
- Growth Enterprise plan restrictions