## Summary
The Identify API allows setting User IDs for Device IDs or updating user properties without sending events, supporting various operations like $set, $setOnce, $add, $append, and $unset, with throttling for requests exceeding thresholds and no impact on active user counts or monthly event quotas.

## Keywords
- user_properties
- device_id
- user_id
- identification
- property operations
- $set
- $setOnce
- $add
- $append
- throttling
- non-retroactive updates
- groups
- HTTP 429