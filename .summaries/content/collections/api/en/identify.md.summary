## Summary
The Identify API allows setting User IDs for Device IDs or updating user properties without sending events, supporting operations like $set, $setOnce, $add, $append, $prepend, $unset, $preInsert, $postInsert, and $remove on user properties, with updates applying only to future events.

## Keywords
- User ID mapping
- Device ID association
- User property operations
- Non-event updates
- Request throttling
- Property modification
- $set/$setOnce/$add operations
- Form-data POST requests
- Enterprise groups feature
- User property arrays