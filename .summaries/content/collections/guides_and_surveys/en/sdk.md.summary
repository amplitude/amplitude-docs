**Summary:** This documentation covers the Amplitude Guides and Surveys Web SDK implementation, including installation methods (Amplitude Browser SDK plugin, third-party analytics integration, GTM), initialization with `init()` and `boot()` functions, and SDK methods like `forwardEvent()`, `setThemeMode()`, `addCallback()`, and `setRouter()` for SPA navigation.

**Keywords:**
- @amplitude/engagement-browser
- engagement.init()
- engagement.boot()
- window.engagement
- BootOptions
- InitOptions
- forwardEvent()
- setThemeMode()
- addCallback()
- setRouter()
- engagement.gs.show()
- engagement.gs.reset()
- engagement.gs.list()
- engagement.gs.closeAll()
- _setUserProperties()
- updateLanguage()
- serverZone
- serverUrl
- cdnUrl
- nonce
- CSP
- integrations
- user_id
- device_id
- event_type
- event_properties
- ThemeMode
- Segment analytics
- Google Tag Manager
- SPA router
- localization
- targeting resolution
- _debugStatus()