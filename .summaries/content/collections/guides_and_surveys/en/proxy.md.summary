## Summary
Documents setting up AWS CloudFront as a reverse proxy for Amplitude Guides and Surveys, configuring a unified distribution with two origins (cdn.amplitude.com for static assets and gs.amplitude.com for API requests) and specific cache behaviors including the critical `/sdk/*` path pattern for API routing.

## Keywords
- AWS CloudFront reverse proxy
- Guides and Surveys API proxy
- cdn.amplitude.com origin
- gs.amplitude.com origin
- `/sdk/*` path pattern
- cache behaviors configuration
- serverUrl and cdnUrl initialization
- preview mode `/sdk/admin/config`
- POST requests state persistence
- AllViewerExceptHostHeader policy
- CachingDisabled API policy
- engagement.init() proxy configuration