**Summary:** This documentation covers Amplitude's Guides and Surveys setup and targeting configuration, including user segmentation, trigger options (element appearance/clicks, time delays, rage clicks, user confusion, event tracking), session properties, page targeting with URL matching patterns, priority levels, display limits, localization, status management (draft/published/scheduled), and active state behavior with tiebreaker rules.

**Keywords:**
- Amplitude Guides and Surveys
- User targeting and segmentation
- Trigger configuration (element appears, element clicked, time delays, smart delay, rage click, user confusion, event tracking)
- Session properties (setSessionProperty SDK method)
- Page targeting (URL matching patterns, regex)
- Priority levels (urgent, high, medium, low)
- Display limits and cooldown periods
- Guide localization
- Status management (draft, published, scheduled)
- Active state and tiebreaker rules
- CSS selectors and XPath expressions
- Visual selector tool
- Rollout percentage and bucketing
- Query parameter sharing
- Step counter and dismissable settings