# Amplitude Migration Guide

## Product Overview

Amplitude is an analytics platform for tracking and analyzing user behavior data. The migration documentation provides comprehensive guidance for transitioning from various analytics platforms to Amplitude, including Adobe Analytics, Google Analytics, Mixpanel, mParticle, and Segment. The guides focus on preserving data continuity, mapping existing tracking implementations, and ensuring proper validation of migrated data.

## Key Features and Concepts

- **Cross-Platform Migration Support**: Detailed migration paths from major analytics platforms
- **Multiple Migration Approaches**: Options for both low-code/no-code migrations and complete SDK replacements
- **Historical Data Migration**: Methods to backfill historical data to maintain reporting continuity
- **Data Validation Tools**: Built-in validation mechanisms like Ingestion Debugger and User Lookup
- **SDK Implementation**: Cross-platform SDK support for Browser, iOS, and Android
- **Data Taxonomy Mapping**: Guidance on mapping existing data models to Amplitude's event-based schema

## Product Relationships and Features

### Migration Pathways

1. **Adobe Analytics to Amplitude**:
   - Maps Adobe's variable system (eVars, props) to Amplitude's event properties
   - Provides client-side and server-side implementation strategies
   - Includes report replication guidance

2. **Google Analytics (GA4) to Amplitude**:
   - Offers a low-code Google Analytics event forwarding plugin
   - Provides a BigQuery Import beta option
   - Includes code replacement examples for converting gtag functions to Amplitude SDK calls

3. **Mixpanel to Amplitude**:
   - Features a dedicated Mixpanel import tool for historical data
   - Provides industry-specific use case recommendations
   - Includes detailed method mapping between platforms

4. **mParticle to Amplitude**:
   - Focuses on source/destination configuration
   - Provides SDK implementation conversion examples
   - Includes user identity mapping guidance

5. **Segment to Amplitude**:
   - Covers source/destination configuration
   - Provides method conversion examples (identify, track, group)
   - Includes cross-platform SDK implementation guidance

## Key Nomenclature and Definitions

- **Event Properties**: Contextual data points equivalent to eVars in Adobe or custom dimensions in Google Analytics
- **User Properties**: Persistent attributes associated with a user across sessions
- **Identify Method**: Function to associate events with specific user identities
- **Track Method**: Primary function to record events in Amplitude
- **Group Method**: Function to associate users with organizational entities
- **Ingestion Debugger**: Tool for validating proper event tracking implementation
- **User Lookup**: Tool for verifying user identity and associated events
- **Tracking Plan**: Documentation outlining events, properties, and user attributes to track
- **Backfill**: Process of importing historical data from a previous analytics platform
- **Data Taxonomy**: Organizational structure of events, properties, and user attributes

## Product Ecosystem Integration

Amplitude integrates with the broader analytics ecosystem through:

- **Direct SDK Implementation**: Native SDKs for Browser, iOS, and Android platforms
- **CDP Integration**: Compatibility with Customer Data Platforms like Segment and mParticle
- **Tag Management Systems**: Support for tag managers mentioned in the Adobe migration documentation
- **Server-Side Sources**: Ability to receive data from server-side implementations
- **Data Import Tools**: Specialized tools for importing historical data from other platforms

## API and Implementation Examples

### Browser SDK Implementation

```javascript
// Initializing Amplitude
amplitude.init('YOUR_API_KEY');

// Tracking an event
amplitude.track('Button Clicked', {
  buttonColor: 'blue',
  buttonText: 'Submit'
});

// Identifying a user
amplitude.identify(new amplitude.Identify()
  .set('email', 'user@example.com')
  .set('plan', 'premium'));
```

### iOS SDK Implementation

```swift
// Initializing Amplitude
Amplitude.instance().initializeApiKey("YOUR_API_KEY")

// Tracking an event
Amplitude.instance().logEvent("Button Clicked", withEventProperties: [
  "buttonColor": "blue",
  "buttonText": "Submit"
])

// Identifying a user
let identify = AMPIdentify()
identify.set("email", value: "user@example.com")
identify.set("plan", value: "premium")
Amplitude.instance().identify(identify)
```

### Android SDK Implementation

```java
// Initializing Amplitude
Amplitude.getInstance().initialize(getApplicationContext(), "YOUR_API_KEY");

// Tracking an event
JSONObject eventProperties = new JSONObject();
eventProperties.put("buttonColor", "blue");
eventProperties.put("buttonText", "Submit");
Amplitude.getInstance().logEvent("Button Clicked", eventProperties);

// Identifying a user
Identify identify = new Identify()
  .set("email", "user@example.com")
  .set("plan", "premium");
Amplitude.getInstance().identify(identify);
```

Each migration guide provides platform-specific implementation details for converting from source platforms to Amplitude equivalents, ensuring smooth transitions while maintaining data integrity and reporting continuity.