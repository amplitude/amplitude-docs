**Summary:** Documents Amplitude's session length calculation formula using max(client_event_time) - min(client_event_time) based on session ID property, excluding hidden/inactive/deleted events from the calculation.

**Keywords:**
- session length calculation
- client_event_time
- session ID property
- max/min timestamp formula
- UTC local timestamp
- hidden events exclusion
- inactive events exclusion
- deleted events exclusion
- session tracking
- event filtering