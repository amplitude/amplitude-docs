## Amplitude Browser SDK Product Suite

Amplitude's Browser SDK is a comprehensive analytics tracking solution for web applications, providing multiple SDK variants and tools for event tracking, user analytics, and marketing attribution. The product suite centers around strongly-typed JavaScript/TypeScript SDKs that integrate with Amplitude's analytics platform through the HTTP V2 API.

## Product Relationships and Architecture

The Browser SDK ecosystem consists of several interconnected products:

**Core SDKs:**
- **Browser SDK 2.0** (`@amplitude/analytics-browser`) - The current flagship SDK with built-in autocapture functionality
- **Browser SDK 1.0** (`@amplitude/analytics-browser@1.x`) - Legacy version in maintenance mode
- **JavaScript SDK** (`amplitude-js`) - Original legacy SDK in maintenance mode
- **Marketing Analytics SDK** (`@amplitude/marketing-analytics-browser`) - Specialized SDK for marketing attribution (maintenance mode)

**Unified Platform:**
- **Browser Unified SDK** (`@amplitude/unified`) - Single entry point for Analytics, Experiment, and Session Replay integration

**Code Generation Tools:**
- **Ampli Wrappers** - Strongly-typed API generators for Browser SDK 1.0, 2.0, and JavaScript SDK versions

**Extensions:**
- **Autocapture Plugin** - Deprecated standalone plugin (functionality now built into Browser SDK 2.0)

## Key Nomenclature and Definitions

**Core Concepts:**
- **Ampli Wrapper** - Code generation tool that creates strongly-typed TypeScript/JavaScript APIs from Amplitude Data tracking plans
- **Autocapture** - Automatic tracking of user interactions including page views, sessions, form interactions, file downloads, and element interactions
- **Attribution Tracking** - Marketing campaign attribution via UTM parameters, click IDs (gclid, fbclid, dclid), and referrer tracking
- **Device ID/User ID/Session ID** - Core identity management identifiers for user tracking
- **CJS Module** - CommonJS module packaging format used across SDK variants

**API Methods:**
- `amplitude.init()` - SDK initialization with API key and configuration
- `amplitude.track()` - Event tracking with properties
- `amplitude.identify()` - User property management
- `amplitude.flush()` - Force event queue transmission
- `ampli.load()` - Ampli wrapper initialization
- `ampli.pull` - CLI command to generate tracking code from Data plans
- `ampli.status` - CLI command to verify tracking plan synchronization

**Configuration Options:**
- `serverZone` - Data residency (US/EU) configuration
- `cookieOptions`/`identityStorage` - Cookie and storage management
- `defaultTracking` - Autocapture feature configuration
- `flushQueueSize`/`flushIntervalMillis` - Event batching parameters
- `trackingOptions` - Field-level tracking controls
- `sendBeacon` - Browser transport mechanism for reliable event delivery

## Broader Product Ecosystem Integration

The Browser SDK integrates with Amplitude's broader analytics ecosystem:

**Data Platform Integration:**
- **Amplitude Data** - Tracking plan management and schema validation
- **HTTP V2 API** - Backend event ingestion endpoint
- **Batch API** - Optimized bulk event transmission

**Product Suite Integration:**
- **Amplitude Analytics** - Core event tracking and user analytics
- **Amplitude Experiment** - Feature flagging and A/B testing
- **Session Replay** - User session recording and analysis

**Development Workflow:**
- **Amplitude CLI** - Command-line interface for Ampli code generation
- **Visual Labeling** - UI-based event configuration
- **Remote Configuration** - Server-side SDK configuration management

## API Endpoints and CLI Commands

**CLI Commands:**
- `ampli pull` - Generate strongly-typed tracking code from Data tracking plans
- `ampli status` - Verify tracking implementation against tracking plan schema

**Key API Methods:**
- `initAll()` - Unified SDK initialization for multiple Amplitude products
- `setGroup()` - Organization/group-level analytics assignment
- `logRevenueV2()` - Revenue event tracking (legacy JavaScript SDK)
- `getInstance()` - Multi-instance management (legacy JavaScript SDK only)

**Configuration Endpoints:**
- Cross-domain tracking via shared device ID
- EU data residency through `serverZone: 'EU'`
- Content Security Policy (CSP) compliance configurations

The product suite emphasizes migration paths from legacy SDKs to Browser SDK 2.0, with comprehensive autocapture capabilities, unified product integration, and strongly-typed development workflows through Ampli code generation.