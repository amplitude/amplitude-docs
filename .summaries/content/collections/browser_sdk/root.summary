# Amplitude Browser SDK Product Suite

Amplitude's Browser SDK is a comprehensive web analytics platform providing multiple SDK variants, development tools, and specialized plugins for event tracking, user analytics, and marketing attribution across web applications.

## Product Relationships and Architecture

The Browser SDK ecosystem operates through a tiered architecture of interconnected products:

**Core SDK Hierarchy:**
- **Browser SDK 2.0** (`@amplitude/analytics-browser@2+`) - Current flagship with native autocapture and marketing attribution
- **Browser SDK 1.0** (`@amplitude/analytics-browser@1.x`) - Legacy maintenance version
- **JavaScript SDK** (`amplitude-js`) - Original legacy SDK in maintenance mode
- **Marketing Analytics SDK** (`@amplitude/marketing-analytics-browser`) - Specialized attribution SDK (maintenance mode)
- **Browser Unified SDK** (`@amplitude/unified`) - Beta integration hub for Analytics, Experiment, and Session Replay

**Development Ecosystem:**
- **Ampli Wrappers** - Code generation framework creating strongly-typed APIs from Amplitude Data tracking plans
- **Plugin Architecture** - Extensible system for data enrichment and custom destinations
- **Migration Tools** - Automated upgrade paths between SDK versions

## Key Nomenclature and Definitions

**Identity Management:**
- **Device ID** - Unique device identifier stored in cookies with `AMP_` prefix
- **User ID** - Application-specific authenticated user identifier
- **Session ID** - Temporary session identifier with configurable timeout
- **identityStorage** - Cookie and localStorage persistence configuration

**Event System:**
- **Event Properties** - Custom metadata attached to tracked events
- **User Properties** - Persistent user attributes managed via `Identify` interface
- **Group Properties** - Organization-level B2B analytics attributes
- **Revenue Interface** - Dedicated monetization tracking system

**Technical Configuration:**
- **HTTP V2 API** - Primary event ingestion endpoint
- **serverZone** - Data residency control (US/EU)
- **flushQueueSize/flushIntervalMillis** - Event batching parameters
- **trackingOptions** - Granular data collection controls
- **sendBeacon** - Browser API for reliable event transmission
- **CJS Module** - CommonJS packaging format

**Marketing Attribution:**
- **UTM Parameters** - Campaign tracking (utm_source, utm_medium, utm_campaign, etc.)
- **Click IDs** - Platform identifiers (gclid, fbclid, dclid, ttclid)
- **First-touch/Multi-touch Attribution** - Attribution model configurations
- **Cross-domain Tracking** - Device ID persistence across domains

## Broader Product Ecosystem Integration

The Browser SDK functions as the web analytics foundation within Amplitude's unified customer data platform:

**Amplitude Data Platform:**
- Centralized tracking plan management through Ampli code generation
- Schema validation and type safety enforcement
- Real-time implementation status monitoring

**Multi-Product Convergence:**
- **Session Replay** - User behavior recording via Unified SDK
- **Experiment** - Feature flag management and A/B testing
- **Revenue Analytics** - Monetization tracking with dedicated interfaces
- **Group Analytics** - B2B organizational insights

**Data Infrastructure:**
- HTTP V2 API event ingestion with EU data residency support
- Offline queuing with automatic retry mechanisms
- Remote configuration management
- Plugin-based data pipeline extensibility

## API Endpoints and CLI Commands

**Core SDK Interface:**
```javascript
amplitude.init(apiKey, options)                    // SDK initialization
amplitude.track(eventType, eventProperties)       // Event tracking
amplitude.identify(identifyObject)                // User property updates
amplitude.setGroup(groupType, groupValue)         // Group assignment
amplitude.flush()                                 // Force queue transmission
```

**Ampli Development Workflow:**
```bash
ampli pull [--path ./src/ampli]    # Download tracking plan
ampli status                       # Verify implementation compliance
```

**Plugin System:**
```javascript
amplitude.add(plugin)              // Plugin registration
// Enrichment plugins - data transformation
// Destination plugins - custom routing
```

**Configuration Management:**
- `cookieOptions` - Cookie persistence settings
- `defaultTracking` - Autocapture behavior configuration
- `trackingOptions` - Data collection field controls
- `transportProvider` - Custom transport implementation

**Migration Infrastructure:**
- Automatic cookie migration from legacy SDK versions
- Backward compatibility layers for seamless upgrades
- Progressive enhancement patterns for feature adoption

The Browser SDK serves as Amplitude's primary web analytics entry point, providing a structured migration path from legacy implementations while delivering modern capabilities including autocapture, attribution modeling, and unified product integration across the Amplitude platform.