# Amplitude Browser SDK Product Suite

Amplitude's Browser SDK is a comprehensive analytics tracking solution for web applications, providing multiple SDK variants and tools for implementing event tracking, user analytics, and marketing attribution in JavaScript and TypeScript environments. The product suite centers around sending analytics events to Amplitude's HTTP V2 API while offering strongly-typed development experiences and extensive customization options.

## Product Relationships and Architecture

The Browser SDK ecosystem consists of several interconnected products organized in an evolutionary hierarchy:

**Core SDK Evolution:**
- **Legacy JavaScript SDK (amplitude-js)** - The original maintenance-mode SDK being phased out
- **Browser SDK 1.0** (@amplitude/analytics-browser v1) - First-generation modern SDK, now in maintenance mode
- **Browser SDK 2.0** (@amplitude/analytics-browser v2+) - Current flagship SDK with enhanced autocapture and attribution features
- **Marketing Analytics SDK** (@amplitude/marketing-analytics-browser) - Specialized maintenance-mode SDK extending Browser SDK 1.0 with marketing attribution

**Development Tools:**
- **Ampli Wrappers** - Code generation tools creating strongly-typed APIs from Amplitude Data tracking plans, available for JavaScript SDK, Browser SDK 1.0, and Browser SDK 2.0
- **Browser Unified SDK** (@amplitude/unified) - Beta integration layer providing single initialization for Analytics, Experiment, and Session Replay

**Extensions and Plugins:**
- **Autocapture Plugin** - Deprecated standalone plugin, now built into Browser SDK 2.0
- **Web Attribution Plugin** - Marketing attribution tracking (V1 in Browser SDK 1.0, V2 in Browser SDK 2.0)
- **Plugin Architecture** - Extensible system supporting enrichment and destination plugins

## Key Nomenclature and Definitions

**Core Concepts:**
- **deviceId/userId** - Primary user identification mechanisms with configurable minimum length requirements (minIdLength)
- **sessionId** - Session tracking identifier with configurable timeout (sessionTimeout)
- **API Key** - Project identifier used for SDK initialization and cookie scoping
- **HTTP V2 API** - Amplitude's current event ingestion endpoint used by modern SDKs

**Event Tracking:**
- **track()** - Primary event logging method (replaces legacy logEvent())
- **identify()** - User property management with operations (set, setOnce, add, append, prepend)
- **Revenue** - Specialized tracking interface for monetary events with productId, price, quantity
- **flush()** - Manual queue processing (replaces legacy sendEvents())

**Configuration Systems:**
- **defaultTracking/autocapture** - Automated event collection for pageViews, sessions, formInteractions, fileDownloads, elementInteractions, networkTracking
- **cookieOptions** - Cookie management configuration including domain, expiration, secure, sameSite
- **identityStorage** - Storage mechanism selection (cookies, localStorage, sessionStorage, none)
- **transportProvider** - HTTP transport configuration including sendBeacon support

**Data Management:**
- **flushQueueSize/flushIntervalMillis** - Batch processing parameters
- **trackingOptions** - Granular control over automatic data collection
- **serverZone** - Data residency configuration (US/EU)
- **AMP_ Cookie Prefix** - Naming convention for first-party cookies scoped by API key

## Broader Product Ecosystem Integration

The Browser SDK integrates with Amplitude's comprehensive analytics platform through multiple touchpoints:

**Data Pipeline Integration:**
- Events flow through Amplitude's HTTP V2 API to the core analytics platform
- **Amplitude Data** integration via Ampli wrappers provides schema validation and strongly-typed event definitions
- **EU Data Residency** support through serverZone configuration ensures compliance with regional requirements

**Cross-Product Functionality:**
- **Session Replay** integration through Browser Unified SDK and Browser SDK 2.0 compatibility
- **Amplitude Experiment** feature flagging integration via unified initialization
- **Marketing Attribution** connects with campaign tracking and UTM parameter management
- **User Privacy APIs** including DSAR (Data Subject Access Request) support for GDPR compliance

**Development Ecosystem:**
- **Content Security Policy (CSP)** compatibility for secure web environments
- **Cross-domain tracking** via ampDeviceId and ampSessionId URL parameters
- **Google Tag Manager (GTM)** integration patterns
- **RequireJS** module loading support
- **Tree shaking** optimization for bundle size management

## API Endpoints and CLI Commands

**Ampli CLI Commands:**
- `ampli pull` - Downloads tracking plan and generates strongly-typed SDK code
- `ampli status` - Verifies implementation against tracking plan schema

**Core SDK Methods:**
- `amplitude.init(apiKey, options)` - SDK initialization
- `amplitude.track(eventType, eventProperties)` - Event tracking
- `amplitude.identify(identifyObject)` - User property management  
- `amplitude.setUserId(userId)` - User identification
- `amplitude.setGroup(groupType, groupName)` - User group assignment
- `amplitude.flush()` - Manual event queue processing
- `amplitude.reset()` - User session reset and anonymization

**Configuration APIs:**
- `amplitude.setOptOut(optOut)` - Privacy opt-out management
- `amplitude.add(plugin)` - Plugin registration
- `fetchRemoteConfig()` - Remote configuration retrieval
- `createInstance(instanceName)` - Multiple SDK instance creation

**Legacy JavaScript SDK Methods:**
- `amplitude.getInstance()` - Instance retrieval (deprecated in modern SDKs)
- `amplitude.logEvent()` - Event logging (replaced by track())
- `amplitude.logRevenueV2()` - Revenue tracking (replaced by Revenue interface)

The Browser SDK ecosystem represents Amplitude's evolution toward modern web analytics with enhanced developer experience, comprehensive privacy controls, and seamless integration with the broader Amplitude platform while maintaining backward compatibility through migration paths and maintenance support for legacy implementations.