## Amplitude Browser SDK Product Suite

Amplitude's Browser SDK is a comprehensive analytics tracking solution for web applications, providing multiple SDK variants and tools for event tracking, user analytics, and marketing attribution. The product suite includes several SDK versions, wrapper tools, and specialized plugins designed for different use cases and migration paths.

## Product Relationships and Architecture

The Browser SDK ecosystem consists of several interconnected products:

**Core SDKs:**
- **Browser SDK 2.0** (`@amplitude/analytics-browser@2+`) - The current flagship SDK with built-in autocapture and marketing attribution
- **Browser SDK 1.0** (`@amplitude/analytics-browser@1.x`) - Legacy version in maintenance mode
- **JavaScript SDK** (`amplitude-js`) - Original legacy SDK in maintenance mode
- **Marketing Analytics SDK** (`@amplitude/marketing-analytics-browser`) - Specialized SDK for marketing attribution, now in maintenance mode
- **Browser Unified SDK** (`@amplitude/unified`) - Beta integration providing single entry point for Analytics, Experiment, and Session Replay

**Development Tools:**
- **Ampli Wrappers** - Code generation tools that create strongly-typed APIs from Amplitude Data tracking plans, available for all SDK versions

**Plugins:**
- **Autocapture Plugin** - Deprecated standalone plugin, now built into Browser SDK 2.0
- **Web Attribution Plugin** - Marketing attribution tracking capabilities
- **Enrichment and Destination Plugins** - Extensible plugin architecture for data processing

## Key Nomenclature and Definitions

**Core Concepts:**
- **Device ID** - Unique identifier for user devices, managed via cookies with `AMP_` prefix
- **User ID** - Application-specific user identifier for authenticated users
- **Session ID** - Temporary identifier for user sessions with configurable timeout
- **Event Properties** - Custom data attached to tracked events
- **User Properties** - Persistent attributes associated with users via `Identify` interface
- **Group Properties** - Organization-level attributes for B2B analytics

**Technical Terms:**
- **HTTP V2 API** - Amplitude's current event ingestion endpoint
- **CJS Module** - CommonJS module packaging format
- **sendBeacon** - Browser API for reliable event transmission
- **flushQueueSize/flushIntervalMillis** - Batching configuration parameters
- **trackingOptions** - Configuration to disable specific data collection fields
- **serverZone** - Data residency configuration (US/EU)
- **identityStorage** - Cookie and localStorage management settings

**Marketing Attribution:**
- **UTM Parameters** - Campaign tracking parameters (utm_source, utm_medium, etc.)
- **Click IDs** - Platform-specific tracking identifiers (gclid, fbclid, dclid)
- **First-touch/Multi-touch Attribution** - Attribution model configurations
- **Cross-domain Tracking** - Device ID persistence across domains

## Broader Product Ecosystem Integration

The Browser SDK integrates with Amplitude's broader analytics platform through several touchpoints:

**Amplitude Data Integration:**
- Ampli wrappers generate code from centralized tracking plans
- Schema validation and type safety enforcement
- CLI commands: `ampli pull`, `ampli status` for plan synchronization

**Multi-Product Integration:**
- **Session Replay** integration via Unified SDK
- **Experiment** (feature flagging) integration via Unified SDK
- **Revenue Tracking** with dedicated `Revenue` interface
- **Group Analytics** for B2B use cases

**Data Pipeline:**
- Events sent to HTTP V2 API endpoint
- Support for EU data residency via serverZone configuration
- Offline mode with local queuing
- Remote configuration capabilities

## API Endpoints and CLI Commands

**Core SDK Methods:**
- `amplitude.init(apiKey, options)` - SDK initialization
- `amplitude.track(eventType, eventProperties)` - Event tracking
- `amplitude.identify(identifyObject)` - User property updates
- `amplitude.flush()` - Force event queue transmission
- `amplitude.setGroup(groupType, groupValue)` - Group assignment

**Ampli CLI Commands:**
```bash
ampli pull [--path ./src/ampli]  # Download tracking plan
ampli status                     # Verify implementation status
```

**Plugin System:**
- `amplitude.add(plugin)` - Plugin registration
- Enrichment plugins for data transformation
- Destination plugins for custom event routing

**Configuration APIs:**
- `cookieOptions` - Cookie management settings
- `defaultTracking` - Autocapture configuration
- `trackingOptions` - Data collection controls
- `transportProvider` - Custom transport implementation

**Migration Utilities:**
- Automatic cookie migration from legacy SDKs
- Backward compatibility layers
- Progressive enhancement patterns

The Browser SDK serves as Amplitude's primary web analytics solution, offering a migration path from legacy implementations while providing modern features like autocapture, marketing attribution, and unified product integration.