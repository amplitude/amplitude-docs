## Amplitude Browser SDK Product Suite

The Amplitude Browser SDK is a comprehensive analytics tracking solution for web applications, providing multiple SDK variants and tools for implementing event tracking, user analytics, and marketing attribution in JavaScript and TypeScript environments. The product suite includes legacy SDKs in maintenance mode, current generation SDKs, and specialized tools for type-safe analytics implementation.

## Product Relationships and Architecture

The Browser SDK ecosystem consists of several interconnected products:

**Core SDKs:**
- **Browser SDK 2.0** (`@amplitude/analytics-browser@2+`) - The current flagship SDK using HTTP V2 API with built-in autocapture, marketing attribution, and modern plugin architecture
- **Browser SDK 1.0** (`@amplitude/analytics-browser@1.x`) - Previous generation SDK in maintenance mode
- **Legacy JavaScript SDK** (`amplitude-js`) - Original SDK in maintenance mode, being phased out

**Specialized Tools:**
- **Ampli Wrapper** - Code generation tool that creates strongly-typed TypeScript/JavaScript APIs from Amplitude Data tracking plans, available for all SDK versions
- **Marketing Analytics SDK** (`@amplitude/marketing-analytics-browser`) - Maintenance-mode SDK extending Browser SDK with enhanced marketing attribution features
- **Unified SDK** (`@amplitude/unified`) - Beta integration providing single entry point for Analytics, Experiment, and Session Replay

**Plugins and Extensions:**
- **Autocapture Plugin** - Deprecated standalone plugin, now built into Browser SDK 2.10.0+
- **Web Attribution Plugin** - Marketing attribution tracking integrated into core SDKs

## Key Nomenclature and Definitions

**Core Concepts:**
- **deviceId** - Unique identifier for device/browser, stored in cookies with AMP_{api_key} prefix
- **userId** - User identifier for cross-device tracking, Base64 encoded in storage
- **sessionId** - Session identifier with configurable timeout (default 30 minutes)
- **Autocapture** - Automatic tracking of page views, sessions, form interactions, file downloads, and element interactions
- **HTTP V2 API** - Current Amplitude ingestion endpoint used by modern SDKs

**Configuration Options:**
- **serverZone** - Data residency setting (US/EU) for GDPR compliance
- **identityStorage** - Storage mechanism (cookies/localStorage/sessionStorage/none)
- **cookieOptions** - Cookie configuration including expiration, secure, sameSite settings
- **flushQueueSize/flushIntervalMillis** - Event batching and transmission settings
- **trackingOptions** - Granular control over automatic data collection

**Event Tracking:**
- **track()** - Primary method for logging custom events with properties
- **identify()** - User property management using Identify interface
- **Revenue** - Specialized interface for e-commerce tracking via logRevenueV2()
- **defaultTracking** - Configuration for automatic event collection

**Plugin Architecture:**
- **EnrichmentPlugin** - Modifies events before transmission
- **DestinationPlugin** - Handles event delivery to endpoints
- **Plugin Interface** - Extensible architecture for custom functionality

## Ecosystem Integration

The Browser SDK integrates with the broader Amplitude ecosystem through:

**Data Management:**
- **Amplitude Data** - Tracking plan service that generates Ampli wrapper code
- **GDPR/CNIL Compliance** - Built-in consent management and privacy controls
- **Cross-domain Tracking** - Shared deviceId across subdomains

**Development Tools:**
- **Ampli CLI** - Command-line tool with `pull` and `status` commands for tracking plan synchronization
- **TypeScript Support** - Full type definitions and strongly-typed event tracking
- **JSDoc Integration** - Type checking for JavaScript implementations

**Advanced Features:**
- **Session Replay** - Integration through Unified SDK
- **A/B Testing** - Experiment SDK integration
- **Marketing Attribution** - UTM parameters, click IDs (gclid, fbclid, dclid), referrer tracking
- **Offline Mode** - Event queuing and retry mechanisms

## API Endpoints and CLI Commands

**Primary API Methods:**
- `amplitude.init(apiKey, options)` - SDK initialization
- `amplitude.track(eventType, eventProperties)` - Event logging
- `amplitude.identify(identifyObject)` - User property updates
- `amplitude.flush()` - Force event transmission
- `amplitude.reset()` - Clear user data and reset state

**Ampli CLI Commands:**
- `ampli pull` - Download latest tracking plan and generate code
- `ampli status` - Verify implementation against tracking plan

**Configuration APIs:**
- `amplitude.setOptOut(boolean)` - Privacy control
- `amplitude.setGroup(groupType, groupName)` - User grouping
- `fetchRemoteConfig()` - Dynamic configuration retrieval

**Installation Methods:**
- Script loader implementation for CDN delivery
- npm/yarn package installation for bundled applications
- CJS module support for CommonJS environments

The Browser SDK represents Amplitude's comprehensive solution for web analytics, providing migration paths from legacy implementations while offering modern features like autocapture, enhanced privacy controls, and seamless integration with the broader Amplitude platform ecosystem.