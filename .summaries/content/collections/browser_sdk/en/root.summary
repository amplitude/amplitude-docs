## Amplitude Browser SDK Product Suite

Amplitude's Browser SDK is a comprehensive analytics tracking solution for web applications, providing multiple SDK variants and tools for implementing event tracking, user analytics, and marketing attribution in JavaScript and TypeScript environments.

## Product Architecture and Relationships

The Browser SDK ecosystem consists of several interconnected products:

**Core SDKs:**
- **Browser SDK 2.0** (`@amplitude/analytics-browser@2+`) - The current flagship SDK using HTTP V2 API
- **Browser SDK 1.0** (`@amplitude/analytics-browser@1.x`) - Legacy version in maintenance mode
- **JavaScript SDK** (`amplitude-js`) - Original legacy SDK in maintenance mode
- **Marketing Analytics SDK** (`@amplitude/marketing-analytics-browser`) - Specialized SDK for marketing attribution (maintenance mode)
- **Browser Unified SDK** (`@amplitude/unified`) - Beta integration combining Analytics, Experiment, and Session Replay

**Developer Tools:**
- **Ampli Wrappers** - Code generation tools that create strongly-typed TypeScript/JavaScript APIs from Amplitude Data tracking plans, available for all SDK versions
- **Autocapture Plugin** - Automated event tracking functionality (now built into Browser SDK 2.10.0+)

## Key Nomenclature and Definitions

**Core Concepts:**
- **deviceId** - Unique identifier for user devices, persisted in cookies with AMP_{api_key} naming convention
- **userId** - Custom user identifier for authenticated users
- **sessionId** - Temporary identifier for user sessions with configurable timeout
- **Autocapture** - Automated tracking of page views, sessions, form interactions, file downloads, and element interactions
- **Default Tracking** - Built-in event collection including page views, sessions, marketing attribution, and user interactions

**Technical Terms:**
- **HTTP V2 API** - Amplitude's current event ingestion endpoint used by modern SDKs
- **CJS Module** - CommonJS module packaging format for Node.js compatibility
- **sendBeacon** - Browser API for reliable event transmission during page unload
- **Cross-domain tracking** - Sharing user identity across different domains
- **Server Zone** - Data residency configuration (US/EU) for GDPR compliance

**Configuration Options:**
- **flushQueueSize** - Number of events to batch before sending
- **flushIntervalMillis** - Time interval for automatic event transmission
- **identityStorage** - Storage mechanism for user identity (cookies, localStorage, sessionStorage, none)
- **trackingOptions** - Granular control over automatic data collection
- **cookieOptions** - Cookie configuration including expiration, security, and SameSite settings

## Broader Product Ecosystem Integration

The Browser SDK integrates with Amplitude's broader analytics and experimentation platform:

**Data Management:**
- **Amplitude Data** - Schema management and tracking plan validation system that generates Ampli wrapper code
- **User Privacy API/DSAR API** - Data subject access and deletion compliance tools

**Product Integration:**
- **Amplitude Experiment** - Feature flagging and A/B testing platform accessible through unified SDK
- **Session Replay** - User session recording functionality integrated via unified SDK
- **Marketing Attribution** - UTM parameter tracking, referrer analysis, and click ID monitoring (gclid, fbclid, dclid)

**Compliance and Privacy:**
- **GDPR compliance** features including deferred initialization and consent management
- **CNIL France exemption** support for analytics cookie requirements
- **Content Security Policy (CSP)** compatibility for secure web applications

## API Endpoints and CLI Commands

**Core SDK Methods:**
- `amplitude.init(apiKey, options)` - SDK initialization
- `amplitude.track(eventType, eventProperties)` - Event tracking
- `amplitude.identify(identifyObject)` - User property management
- `amplitude.flush()` - Force event transmission
- `amplitude.reset()` - Clear user identity and stored data

**Ampli CLI Commands:**
- `ampli pull` - Download and generate tracking code from Amplitude Data
- `ampli status` - Verify tracking plan synchronization

**Configuration APIs:**
- `amplitude.add(plugin)` - Plugin installation
- `amplitude.setGroup(groupType, groupName)` - User group assignment
- `amplitude.setOptOut(optOut)` - Privacy opt-out management

**Legacy JavaScript SDK Methods:**
- `amplitude.getInstance()` - Multi-instance management (deprecated in newer SDKs)
- `amplitude.logEvent()` - Event logging (replaced by `track()`)
- `amplitude.logRevenueV2()` - Revenue tracking

The SDK ecosystem supports multiple installation methods including npm/yarn package management, CDN script loading, and direct browser integration, with comprehensive migration paths between versions and specialized tooling for enterprise-scale implementations.