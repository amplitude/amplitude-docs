## Amplitude Browser SDK Product Suite

The Amplitude Browser SDK is a comprehensive analytics tracking solution for web applications, offering multiple SDK versions and specialized tools for event tracking, user analytics, and marketing attribution. The product suite centers around strongly-typed JavaScript/TypeScript SDKs that integrate with Amplitude's analytics platform using the HTTP V2 API.

## Product Relationships and Architecture

The Browser SDK ecosystem consists of several interconnected products:

**Core SDKs:**
- **Browser SDK 2.0** (`@amplitude/analytics-browser`) - The current flagship SDK with built-in autocapture, advanced attribution tracking, and modern plugin architecture
- **Browser SDK 1.0** (`@amplitude/analytics-browser@1.x`) - Legacy version in maintenance mode with basic tracking capabilities
- **JavaScript SDK** (`amplitude-js`) - Original legacy SDK being phased out in favor of Browser SDK 2.0
- **Marketing Analytics SDK** (`@amplitude/marketing-analytics-browser`) - Specialized SDK for marketing attribution, now in maintenance mode
- **Browser Unified SDK** (`@amplitude/unified`) - Beta integration layer providing single initialization for Analytics, Experiment, and Session Replay

**Code Generation and Type Safety:**
- **Ampli Wrappers** - Strongly-typed API generators available for all SDK versions (1.0, 2.0, JavaScript SDK) that create TypeScript/JavaScript interfaces from Amplitude Data tracking plans

**Extensions and Plugins:**
- **Autocapture Plugin** - Deprecated standalone plugin, now built into Browser SDK 2.10.0+
- **Page URL Enrichment Plugin** (`@amplitude/plugin-page-url-enrichment-browser`) - Automatic page URL property tracking for single-page applications

## Key Nomenclature and Definitions

**Core Concepts:**
- **Amplitude Data tracking plans** - Schema definitions that generate strongly-typed APIs through Ampli
- **HTTP V2 API** - Amplitude's current event ingestion endpoint used by modern SDKs
- **Autocapture** - Automatic tracking of user interactions (page views, sessions, form interactions, file downloads, element interactions)
- **Attribution tracking** - Marketing campaign attribution including UTM parameters, referrer tracking, and click IDs (gclid, fbclid, dclid)

**Configuration and Storage:**
- **deviceId/userId/sessionId** - Core identity management identifiers
- **AMP_ cookie prefix** - Cookie naming convention using API key scoping (e.g., `AMP_{api_key}_deviceId`)
- **identityStorage** - Storage mechanism configuration (cookies, localStorage, sessionStorage, none)
- **serverZone** - Data residency configuration (US/EU)

**Event Management:**
- **flushQueueSize/flushIntervalMillis** - Batch processing configuration
- **sendBeacon** - Browser API for reliable event transmission during page unload
- **trackingOptions** - Granular control over automatic data collection
- **EnrichmentPlugin/DestinationPlugin** - Plugin architecture for data processing and routing

**User and Revenue Tracking:**
- **Identify operations** - User property management (set, setOnce, add, append, prepend)
- **Revenue interface** - E-commerce tracking with productId, price, quantity
- **setGroup()** - Organization/group-level analytics

## Broader Product Ecosystem Integration

The Browser SDK integrates with Amplitude's broader analytics and experimentation platform:

**Data Management:**
- **Amplitude Data** - Schema management and tracking plan generation for Ampli wrappers
- **GDPR/CNIL compliance** - Built-in consent management and privacy controls
- **Cross-domain tracking** - Unified user tracking across multiple domains
- **User Privacy API/DSAR API** - Data subject access and deletion capabilities

**Advanced Analytics:**
- **Session Replay** - User session recording and playback (integrated via Unified SDK)
- **Experiment** - A/B testing and feature flagging (integrated via Unified SDK)
- **Visual labeling** - UI element identification for enhanced analytics

**Development Tools:**
- **Amplitude CLI** - Command-line interface for Ampli operations (`ampli pull`, `ampli status`)
- **Content Security Policy (CSP)** support for secure implementations
- **Multiple framework support** - Angular (Zone.js), React, Vue, and vanilla JavaScript

## API Endpoints and CLI Commands

**Ampli CLI Commands:**
- `ampli pull` - Downloads strongly-typed tracking interfaces from Amplitude Data
- `ampli status` - Verifies tracking plan synchronization

**Core SDK Methods:**
- `amplitude.init()` / `initAll()` - SDK initialization
- `amplitude.track()` - Event tracking
- `amplitude.identify()` - User property management
- `amplitude.flush()` - Force event transmission
- `amplitude.setGroup()` - Group assignment
- `amplitude.reset()` - User anonymization

**Configuration APIs:**
- `cookieOptions.expiration` - Cookie lifecycle management
- `defaultTracking.attribution` - Marketing attribution configuration
- `autocapture.elementInteractions` - User interaction tracking
- `trackingOptions` - Selective data collection controls

The product suite emphasizes migration paths from legacy implementations to Browser SDK 2.0, with comprehensive documentation for upgrading from JavaScript SDK, Browser SDK 1.0, and Marketing Analytics SDK to the current unified architecture.