## Summary

This documentation covers migrating from the legacy amplitude-js SDK to Browser SDK 1.0 (@amplitude/analytics-browser), detailing breaking changes in API methods, configuration options, and architectural differences including the removal of getInstance() pattern, unified track() API replacing multiple logEvent variants, and migration to HTTP V2 API endpoints.

## Keywords

- amplitude-js migration
- @amplitude/analytics-browser
- Browser SDK 1.0
- getInstance() removal
- logEvent to track() API
- HTTP V2 API migration
- plugin architecture
- web attribution changes
- configuration mapping
- flushQueueSize
- serverUrl endpoint
- identify() API changes
- revenue() tracking
- sessionId management
- deviceId requirements
- minIdLength configuration
- sendEvents to flush()
- deferInitialization removal
- pagehide event handling
- beacon transport
- promise-based callbacks