**Summary:** The Browser Unified SDK provides a single entry point for initializing and accessing Amplitude Analytics, Experiment, and Session Replay features through the `@amplitude/unified` package, using `initAll()` for initialization and shared configuration options across all three products.

**Keywords:**
- @amplitude/unified package
- initAll() initialization method
- Browser Unified SDK beta
- Analytics Experiment Session Replay integration
- track identify experiment sessionReplay functions
- serverZone instanceName shared configuration
- sampleRate session replay configuration
- unified TypeScript JavaScript SDK
- single entry point initialization
- bundle size individual product installation