## Summary

This documentation covers the Browser SDK 1 (maintenance mode) for JavaScript/TypeScript, which sends events to Amplitude using the HTTP V2 API with features like default event tracking (page views, sessions, form interactions, file downloads), user properties management, revenue tracking, and plugin architecture for extensibility.

## Keywords

- Browser SDK 1
- @amplitude/analytics-browser
- HTTP V2 API
- maintenance mode
- JavaScript TypeScript
- default event tracking
- page views sessions form interactions file downloads
- deviceId userId minIdLength
- flushQueueSize flushIntervalMillis
- sendBeacon transport
- Identify operations set setOnce add append prepend
- Revenue interface productId price quantity
- plugin architecture enrichment destination
- web attribution plugin
- cookie management AMP prefix
- cross-domain tracking
- CSP content security policy
- trackingOptions optional tracking
- user groups setGroup
- session tracking sessionTimeout
- batch API useBatch
- EU data residency serverZone
- logLevel debugging
- opt-out setOptOut
- reset anonymize users
- custom HTTP client transportProvider
- callback Promise interface
- multiple instances createInstance