**Summary:** Documents Amplitude's Experiment Management API for programmatically creating and controlling feature flags and experiments, including authentication with management API keys, rate limiting (100 req/sec, 100k daily), and CRUD operations across six endpoint categories (flags, experiments, mutex groups, holdouts, deployments, versions).

**Keywords:**
- Experiment Management API
- feature flags programmatic control
- management API key authentication
- Bearer token authorization
- flag CRUD operations
- experiment CRUD operations
- mutex groups API
- holdout groups API
- deployment assignments API
- version endpoints API
- rate limiting 100 requests per second
- cursor-based pagination
- nextCursor parameter
- EU residency server endpoint
- experiment.amplitude.com
- HTTP status codes 200/400/401/403/429