**Summary:** Documents Amplitude Experiment Management API flag endpoints for CRUD operations on feature flags, including flag configuration, variants, versions, deployments, user inclusions, and targeting segments with bucketing and rollout controls.

**Keywords:**
- Amplitude Experiment Management API
- feature flags endpoints
- flag variants management
- user inclusions/exclusions
- rollout weights and percentages
- target segments and conditions
- bucketing configuration (bucketingKey, bucketingSalt, bucketingUnit)
- flag versions and deployments
- evaluation modes (remote/local)
- variant payload management
- flag archiving and enabling
- experiment flag CRUD operations
- amplitude_id bucketing
- flag targeting conditions