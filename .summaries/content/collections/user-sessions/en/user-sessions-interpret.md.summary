**Summary:** This documentation explains how to interpret User Sessions charts in Amplitude, covering chart visualization with segmentation by properties like Country and Carrier, the breakdown table for session/event data, and three session recording methods (server-side via HTTP API v2 with session_id, client-side mobile with 5-minute timeout, client-side web with 30-minute timeout).

**Keywords:**
- User Sessions chart interpretation
- session_id field HTTP API v2
- client-side mobile 5-minute session timeout
- client-side web 30-minute session timeout
- breakdown table segmentation
- property filters before session filters
- Add to Cart event sessions
- Country and Carrier segmentation
- out-of-session flag -1
- custom session property
- milliseconds since epoch session tracking
- Export CSV session data