**Summary:** This documentation explains how to interpret User Sessions charts in Amplitude, covering chart visualization with segmentation by properties like Country and Carrier, the breakdown table with CSV export functionality, and three distinct session recording methods (server-side via HTTP API v2 with session_id, client-side mobile with 5-minute timeout, client-side web with 30-minute timeout).

**Keywords:**
- User Sessions chart interpretation
- session_id field HTTP API v2
- client-side mobile 5-minute session timeout
- client-side web 30-minute session timeout
- breakdown table segmentation
- property filters before session filters
- CSV export functionality
- Carrier and Country segmentation
- out-of-session events session ID -1
- custom session property
- session length calculation
- Add to Cart event filtering