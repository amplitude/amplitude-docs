**Summary:** Documents the Iterable Cohort Sync destination integration that sends Amplitude behavioral cohorts to Iterable's user lists with `[Amplitude]` prefix, using API key authentication and configurable user ID mapping between Amplitude user_id/User Properties and Iterable's userId field.

**Keywords:**
- Iterable Cohort Sync
- Amplitude cohorts export
- Iterable API key authentication
- user_id mapping
- Iterable userId field
- Amplitude User Property mapping
- behavioral cohort synchronization
- Iterable user lists
- [Amplitude] prefix
- marketing automation destination
- sync cadence configuration
- per-project integration enablement