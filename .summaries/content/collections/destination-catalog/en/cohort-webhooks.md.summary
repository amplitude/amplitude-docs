**Summary:** This documentation covers Amplitude CDP's cohort webhook destination that sends cohort updates (user additions/removals) to custom webhook endpoints with configurable payloads using Apache FreeMarker templating, including setup requirements, sync configuration, and payload customization examples.

**Keywords:**
- Amplitude CDP cohort webhooks
- cohort syncing destination
- webhook endpoint configuration
- Apache FreeMarker templating
- cohort updates batching
- user_id and user_properties payload
- in_cohort boolean flag
- message_id deduplication
- computed_time cohort sync
- custom header configuration
- async API pattern recommendations
- 200 OK response requirements
- cohort export sync cadence