**Summary:** Documents Amplitude CDP's cohort webhook destination that sends real-time cohort updates to custom webhook endpoints with configurable Apache FreeMarker templates for payload customization, supporting user properties and custom headers for marketing automation integration.

**Keywords:**
- Amplitude CDP cohort webhooks
- Apache FreeMarker templating
- cohort sync destination
- webhook payload customization
- user_properties cohort export
- cohort_id and cohort_name variables
- in_cohort boolean updates
- message_id deduplication
- computed_time cohort updates
- custom webhook headers configuration
- marketing automation cohort integration