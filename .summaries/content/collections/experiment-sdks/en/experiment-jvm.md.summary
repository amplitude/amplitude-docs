## Summary

This documentation covers Amplitude Experiment's JVM SDK for server-side Java/Kotlin implementations, supporting both remote evaluation (fetching variants via API) and local evaluation (client-side flag evaluation with config polling/streaming and cohort sync capabilities).

## Keywords

- Amplitude Experiment JVM SDK
- Java Kotlin server-side
- remote evaluation fetch variants
- local evaluation flag configs
- deployment key initialization
- ExperimentUser builder
- RemoteEvaluationClient LocalEvaluationClient
- flag config polling streaming
- cohort sync targeting
- assignment tracking
- ServerZone EU data center
- CohortSyncConfig analytics API key
- experiment.fetch() experiment.evaluate()
- variant access FLAG_KEY
- maven-central experiment-jvm-server
- fetchTimeoutMillis flagConfigPollingIntervalMillis
- streamUpdates flag streaming
- account-level bucketing groups
- CompletableFuture variants Map