## Summary

This documentation covers Amplitude Experiment's client-side Android SDK implementation, including installation via Maven Central, initialization with Amplitude Analytics or third-party integrations, remote/local evaluation methods (fetch/start), variant access, exposure tracking, and configuration options for features like automatic exposure tracking, user providers, and bootstrapping with initial variants or flag configurations.

## Keywords

- Amplitude Experiment Android SDK
- client-side Android implementation
- Maven Central installation
- experiment-android-client
- ExperimentClient initialization
- deployment key configuration
- remote evaluation fetch()
- local evaluation start()
- variant() method
- automatic exposure tracking
- ExperimentUser builder
- ExperimentConfig options
- Amplitude Analytics integration
- Segment integration
- ExposureTrackingProvider interface
- ExperimentUserProvider interface
- flag configurations bootstrapping
- initialVariants configuration
- Source.INITIAL_VARIANTS
- ServerZone.EU configuration
- fetchOnStart behavior
- pollOnStart flag updates
- account-level bucketing
- group properties
- variant payload JSONObject
- exposure() manual tracking
- clear() variants cache